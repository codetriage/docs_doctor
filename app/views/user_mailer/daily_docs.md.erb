Hi @<%= @user.github %>,

<% if @write_docs.present? %>
## Write Docs

<% @write_docs.sort_by {|d| d.repo.full_name }.each do |doc| %>
**<%= doc.repo.full_name %>** ([source](<%= doc.to_github %>)): [<%= doc.path %>](<%= doc_method_url doc %>)

<% end %>
<% end %>


<% if @read_docs.present? %>

## Read Docs

<% @read_docs.sort_by {|d| d.repo.full_name }.each do |doc| %>
**<%= doc.repo.full_name %>** ([source](<%= doc.to_github %>)): [<%= doc.path %>](<%= doc_method_url doc %>)

<pre>
<%= doc.doc_comments.first.try(:comment) %>
</pre>

<% end %>
<% end %>

--
DocsDoctor is going away, but don't worry, all the functionality has been ported over to [CodeTriage](https://www.codetriage.com).

We won't be migrating user accounts or settings. If you want to continue to get documentation emails please sign up for an account at [CodeTriage](https://www.codetriage.com) and follow your favorite repos.

Thanks for your interest in making the projects you care about better documented. Keep up the good work by signing up for [CodeTriage](https://www.codetriage.com) where you can help with issues and documentation.

[Delete Account](http://www.docsdoctor.org)
